-- anstyle: styles for terminal text -- colours and effects -- and the ANSI
-- escape sequences that turn them on and off. A port of Rust's `anstyle`
-- crate, version 1.0.14.
--
--   use Anstyle (style, bold, withFg, Ansi, Red, render, renderReset, paint)
--
--   def warning = bold (withFg (Just (Ansi Red)) style)
--   paint warning "careful"  -- "\u{1b}[1m\u{1b}[31mcareful\u{1b}[0m"

mod Color
mod Effects
mod Style
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use Anstyle.Color
@pub use Anstyle.Color.AnsiColor.*
@pub use Anstyle.Color.Color.*
@pub use Anstyle.Effects
@pub use Anstyle.Effects.Effect.*
@pub use Anstyle.Style
